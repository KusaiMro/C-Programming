#include <stdio.h>

int main() 
{
  int i,n;
  int a=0,b=1,next;
  printf("Enter any number:");
  scanf("%d",&n);
  printf("Fibonacchi Seris is:");
  
  for(i=1;i<=n;i++){
      printf(" %d ",a);
      next=a+b;
      a=b;
      b=next;
  }
  
    return 0;
}
