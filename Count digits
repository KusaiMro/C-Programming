#include <stdio.h>

int main() {
  int n,count=0;
  printf("Enter any number:");
  scanf("%d",&n);
  
  if(n<0){
      n = -n;
  }
  if(n==0){
      count=1;
  }
  while(n>0){
     n = n/10;
     count++;
  }
  printf("The digit number of %d is:",count);
    return 0;
}
